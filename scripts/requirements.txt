# LLM Pricing AI Assistant - Requirements
# ==========================================
# NOTE: Requires Python 3.11 or higher (LangGraph + Langfuse requirement)

# Core Web API
fastapi>=0.100.0
uvicorn>=0.23.0

# Tavily Search API
tavily-python>=0.3.0

# ── LangGraph agent framework ──────────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0   # LangChain wrapper for Gemini

# ── Langfuse observability (LLM call tracking) ─────────────────────────────────
langfuse>=2.0.0

# ── Google Gen AI (direct SDK — kept for fallback) ─────────────────────────────
google-genai>=0.2.0

# Data processing
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.31.0

# Development dependencies (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
